"use client";

// Setup toolbar no longer needed since we removed SFCC integration
// This component now simply returns null to avoid breaking existing imports
export const SetupToolbar = () => {
  return null;
};

export default SetupToolbar;
